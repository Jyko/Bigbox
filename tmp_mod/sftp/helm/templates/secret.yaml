apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bigbox-sftp.fullname" . }}-secret
  labels:
    {{- include "bigbox-sftp.labels" . | nindent 4 }}
    app.kubernetes.io/component: secret
type: Opaque
data:
  clientPublicKey: {{ required "🚨 La clé SSH publique du client encodée en base64 est obligatoire : --set sftp.clientPublicKey=# 🚨" .Values.sftp.clientPublicKey | quote }}
